import React, { meno } from "react";

const Header = () => {
    return (
        <header className="header">
            <p className="header-text">Todo App</p>
        </header>
    )
}

export default Header  